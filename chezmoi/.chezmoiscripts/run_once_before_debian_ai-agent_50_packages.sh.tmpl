{{- if and (eq .chezmoi.os "linux") (eq .chezmoi.osRelease.id "debian") (eq .debian_profile "ai-agent") -}}
#!/bin/bash

# "ai-agent" Debian profile — runs on top of the minimal base scripts.
# Add AI tooling here, e.g. node, python, uv, claude-code, etc.

echo "Setting up Debian 'ai-agent' profile..."

{{ end -}}
