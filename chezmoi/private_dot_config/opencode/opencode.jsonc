{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "bash": {
      // "*": "ask",
      "git commit *": "ask",
      "git push *": "ask",
      "rm *": "ask"
    }
  },
  "agent": {
    "ask": {
      "description": "Read-only agent for exploration and analysis - cannot make file changes",
      "mode": "primary",
      "prompt": "{file:./prompts/ask.txt}",
      "permission": {
        "read": {
          "*": "allow",
          "*.env": "deny",
          "*.env.*": "deny",
          "*.env.example": "allow"
        },
        "edit": "deny",
        "bash": {
          "*": "allow",
          "rm *": "deny",
          "git commit *": "deny",
          "git push *": "deny",
          "npm install *": "deny",
          "npm uninstall *": "deny",
          "yarn install *": "deny",
          "yarn remove *": "deny"
        },
        "write": "deny"
      }
    }
  },
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://mcp.atlassian.com/v1/mcp",
      "oauth": {}
    },
    "playwright": {
      "type": "local",
      "command": ["npx", "@playwright/mcp@latest"],
      "enabled": true
    }
  }
}
